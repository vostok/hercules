createStreamRequest:
  type: object
  properties:
    type:
      type: string
      description: stream type
      enum:
        - base
        - derived
    name:
      type: string
    partitions:
      type: integer
    shardingKey:
      type: array
      description: Если то то то
      items:
        type: string
    ttl:
      type: integer
    sources:
      type: string
    filters:
      type: string
createTimelineRequest:
  type: object
  properties:
    name:
      type: string
    slices:
      type: string
    shardingKey:
      description: Если то то то
      type: array
      items:
        type: string
    ttl:
      type: integer
    timetrapSize:
      type: integer
    sources:
      type: string
    filters:
      type: string